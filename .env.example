# 浏览器请求使用的 API 前缀。对接标准 Manabi API 时保持 /api/v1。
VITE_API_BASE_URL=/api/v1

# 仅开发环境使用的 Vite 代理目标：/api 请求会被转发到该地址。
# 指向正在运行的 manabi_api（Docker 默认映射为 127.0.0.1:8001）。
VITE_API_PROXY_TARGET=http://127.0.0.1:8001
